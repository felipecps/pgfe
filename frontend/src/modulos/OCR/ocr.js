/*
import Tesseract from 'tesseract.js';

export function ocr() {
    Tesseract.recognize(
        'paint.jpg',
        'pt',
        { logger: m => console.log(m) }
    ).then(({ data: { text } }) => {
        console.log(text);
    })

}*/